import Login from "@/components/core/modal/Login";

const LoginPage = ({searchParams}: any) => {
    return ( 
        <div>
                <Login searchParams={searchParams}/>

        </div>
    );
}
 
export default LoginPage;
